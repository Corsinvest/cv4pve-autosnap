# This is a template manifest for the FIRST manual submission to WinGet.
# After the first submission is approved, WinGet Releaser will handle all future updates automatically.
#
# Instructions:
# 1. Create a release (e.g., v1.17.0)
# 2. Update <VERSION> and <RELEASE_DATE> in all 3 manifest files
# 3. Download the release files and calculate SHA256 hashes
# 4. Update SHA256 values in the installer manifest
# 5. Submit using: wingetcreate submit <path-to-manifests>
#    OR create a manual PR to microsoft/winget-pkgs

# yaml-language-server: $schema=https://aka.ms/winget-manifest.version.1.6.0.schema.json

PackageIdentifier: Corsinvest.cv4pve.autosnap
PackageVersion: <VERSION>  # e.g., 1.17.0 (without 'v' prefix)
DefaultLocale: en-US
ManifestType: version
ManifestVersion: 1.6.0
